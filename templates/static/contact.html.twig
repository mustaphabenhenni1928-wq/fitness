{% extends 'base.html.twig' %}

{% block title %}Contact - Health & Fitness{% endblock %}

{% block body %}
    <div class="container" style="max-width: 800px; margin: 3rem auto;">
        <h1 style="font-size: 2.5rem; font-weight: 700; margin-bottom: 2rem; text-align: center;">Contactez-nous</h1>

        <div class="card">
            <div class="card-body">
                <p style="margin-bottom: 2rem; color: var(--gray); text-align: center;">
                    Une question ? Un problème ? N'hésitez pas à nous contacter. Nous vous répondrons dans les plus brefs délais.
                </p>

                <form method="POST" action="{{ path('app_contact') }}">
                    <div class="form-group">
                        <label for="name" class="form-label">Nom complet</label>
                        <input type="text" id="name" name="name" class="form-control" required>
                    </div>

                    <div class="form-group">
                        <label for="email" class="form-label">Email</label>
                        <input type="email" id="email" name="email" class="form-control" required>
                    </div>

                    <div class="form-group">
                        <label for="subject" class="form-label">Sujet</label>
                        <input type="text" id="subject" name="subject" class="form-control" required placeholder="Ex: Question sur les entraînements">
                    </div>

                    <div class="form-group">
                        <label for="message" class="form-label">Message</label>
                        <textarea id="message" name="message" class="form-control" rows="6" required placeholder="Votre message..."></textarea>
                    </div>

                    <input type="hidden" name="_token" value="{{ csrf_token('contact') }}">

                    <button type="submit" class="btn btn-primary" style="width: 100%;">
                        Envoyer le message
                    </button>
                </form>

                <div style="margin-top: 3rem; padding-top: 2rem; border-top: 1px solid var(--border-color);">
                    <h3 style="font-size: 1.25rem; margin-bottom: 1rem;">Autres moyens de contact</h3>
                    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1.5rem;">
                        <div>
                            <strong>Email</strong>
                            <p style="color: var(--gray); margin-top: 0.5rem;">contact@healthfitness.com</p>
                        </div>
                        <div>
                            <strong>Support</strong>
                            <p style="color: var(--gray); margin-top: 0.5rem;">support@healthfitness.com</p>
                        </div>
                        <div>
                            <strong>Horaires</strong>
                            <p style="color: var(--gray); margin-top: 0.5rem;">Lun-Ven: 9h-18h</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

