{% extends 'base.html.twig' %}

{% block title %}Gestion des utilisateurs - Administration{% endblock %}

{% block body %}
    <div class="container">
        <h1 style="font-size: 2.5rem; font-weight: 700; margin-bottom: 2rem;">Gestion des utilisateurs</h1>
        
        <div class="card">
            <div class="card-header">
                <h2 class="card-title">Liste des utilisateurs</h2>
            </div>
            <div class="card-body">
                <div class="table-container">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Email</th>
                                <th>Rôle</th>
                                <th>Date d'inscription</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for user in users %}
                                <tr>
                                    <td>{{ user.id }}</td>
                                    <td>{{ user.email }}</td>
                                    <td>
                                        <form method="POST" action="{{ path('app_admin_users') }}" style="display: inline-block; margin: 0;">
                                            <input type="hidden" name="action" value="update_role">
                                            <input type="hidden" name="id" value="{{ user.id }}">
                                            <select name="role" class="form-control" style="display: inline-block; width: auto; padding: 0.25rem 0.5rem; font-size: 0.875rem;" onchange="this.form.submit()">
                                                <option value="ROLE_USER" {% if 'ROLE_USER' in user.roles and 'ROLE_COACH' not in user.roles and 'ROLE_ADMIN' not in user.roles %}selected{% endif %}>Utilisateur</option>
                                                <option value="ROLE_COACH" {% if 'ROLE_COACH' in user.roles %}selected{% endif %}>Coach</option>
                                                <option value="ROLE_ADMIN" {% if 'ROLE_ADMIN' in user.roles %}selected{% endif %}>Administrateur</option>
                                            </select>
                                        </form>
                                    </td>
                                    <td>{{ user.createdAt|date('Y-m-d') }}</td>
                                   
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="5" style="text-align: center; color: var(--gray);">Aucun utilisateur</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}


